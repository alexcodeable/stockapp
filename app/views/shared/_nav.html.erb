<header class="hgt">
  <nav class="navbar navbar-expand-md navbar-light fixed-top bg-light">
    <div class="container">
      <a class="navbar-brand" href="/"> <i class="bi bi-stack text-success"></i> StockApp</a>
      <button class="navbar-toggler collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="navbar-collapse collapse" id="navbarCollapse" style="">
        <ul class="navbar-nav me-auto mb-2 mb-md-0">
          <li class="nav-item">
            <a class="nav-link <%= 'active' if request.path == portfolio_path%>" aria-current="page" href="/portfolio">Portfolio</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="#">Link</a>
          </li>
          <li class="nav-item">
            <a href='/pricing' class="nav-link <%= 'active' if request.path == pricing_path%>">Pricing</a>
          </li>
        </ul>
        <% if user_signed_in?%>
        <ul class="navbar-nav">
           <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle lk" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">
                <%= image_tag(current_user.avatar, width: "30", height: "30", style: "border-radius: 50%") if current_user.avatar.attached? %>
                <%= current_user.full_name %>
              </a>
              <div class="dropdown-menu dropdown-menu-end">
                <a class="dropdown-item" href="#"><%= current_user.full_name %> <br> <span><%= current_user.email %></span></a>
                <div class="dropdown-divider"></div>
                <a class="dropdown-item" href="#"><i class="bi bi-bell"></i> Notifications <span class="badge bg-secondary">4</span></a>
                <a class="dropdown-item text-success" href="/pricing"><i class="bi bi-person-check"></i> Become a pro Member</a>
                <a class="dropdown-item" href="/" target="_blank"><i class="bi bi-life-preserver"></i> Help /Support</a>
                <a class="dropdown-item" href="/profile/edit"><i class="bi bi-gear-wide-connected"></i> User Settings</a>
                <div class="dropdown-divider"></div>
                <a class="dropdown-item text-danger" href="/sign_out"><i class="bi bi-box-arrow-right"></i> Sign Out</a>
              </div>
            </li>
        </ul>
        <% else %>
          <a class='btn btn-outline-success me-2' href="/sign_in">sign in</a>
          <a class='btn btn-success' href="/sign_up">sign up</a>
        <% end %>
        <%
=begin%>
 <form class="d-flex">
          <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
          <button class="btn btn-outline-success" type="submit">Search</button>
        </form> 
<%
=end%>
      </div>
    </div>
  </nav>
</header>